# Accessibility verification

The accessibility protocol moved to the GitHub wiki:

https://github.com/kingabzpro/BetterTradingAgents/wiki/Accessibility

This file remains as a pointer so references in code comments and old links
still resolve. Re-run the manual screen-reader pass after any release that
touches the UI; the automated browser smoke test
(`uv run test browser`) covers the structural checks on every run.
