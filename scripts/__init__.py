"""Offline check scripts and the interactive setup wizard.

Run the fast suite with `uv run test`, one group with `uv run test <name>`,
everything with `uv run test all`, and first-run setup with `uv run setup`.
"""
